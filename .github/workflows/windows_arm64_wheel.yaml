name: Windows ARM64 Wheel

on:
  workflow_dispatch:
  pull_request:
    paths:
      - .github/workflows/windows_arm64_wheel.yaml
      - packaging/build_ffmpeg.sh
      - packaging/build_ffmpeg_arm64.bat
      - packaging/build_libavif.sh
      - packaging/build_libavif_arm64.bat
      - packaging/vc_env_helper_arm64.bat
      - packaging/install_build_dependencies.sh
      - packaging/pre_build_script.sh
      - packaging/post_build_script.sh
      - packaging/install_pytorch.sh
      - packaging/install_torchcodec_wheel.sh
      - packaging/install_test_dependencies.sh
      - packaging/remove_src.sh
      - packaging/assert_ffmpeg_version.py
      - test/smoke_test.py
      - pyproject.toml
      - CMakeLists.txt
      - src/torchcodec/_core/**
  push:
    branches:
      - winarm64-hackathon
    paths:
      - .github/workflows/windows_arm64_wheel.yaml
      - packaging/build_ffmpeg.sh
      - packaging/build_ffmpeg_arm64.bat
      - packaging/build_libavif.sh
      - packaging/build_libavif_arm64.bat
      - packaging/vc_env_helper_arm64.bat
      - packaging/install_build_dependencies.sh
      - packaging/pre_build_script.sh
      - packaging/post_build_script.sh
      - packaging/install_pytorch.sh
      - packaging/install_torchcodec_wheel.sh
      - packaging/install_test_dependencies.sh
      - packaging/remove_src.sh
      - packaging/assert_ffmpeg_version.py
      - test/smoke_test.py
      - pyproject.toml
      - CMakeLists.txt
      - src/torchcodec/_core/**

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref_name }}-${{ github.ref_type == 'branch' && github.sha }}-${{ github.event_name == 'workflow_dispatch' }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build-and-test:
    runs-on: windows-11-arm
    defaults:
      run:
        shell: bash -l -eo pipefail {0}
    env:
      FFMPEG_VERSION: "9.0"
      LIBAVIF_VERSION: "1.4.2"
      TORCHCODEC_BUILD_IMAGE: "0"
      TORCHCODEC_BUILD_HEIC: "0"
      FAIL_WITHOUT_IMAGE_CODECS: "0"
      FAIL_WITHOUT_HEIC: "0"
    steps:
      - name: Check out repo
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Update pip
        run: python -m pip install --upgrade pip

      - name: Install PyTorch
        run: bash packaging/install_pytorch.sh cpu "torch torchvision"

      - name: Install build dependencies
        run: |
          python -m pip install build pybind11 "scikit-build-core>=0.10" ninja

      - name: Build FFmpeg
        run: |
          export FFMPEG_ROOT="${PWD}/ffmpeg"
          packaging/build_ffmpeg_arm64.bat

      - name: Debug FFmpeg pkg-config and libs
        run: |
          ls -R ffmpeg || true
          ls -R ffmpeg/lib/pkgconfig || true
          cat ffmpeg/lib/pkgconfig/libavutil.pc || true
          cat ffmpeg/lib/pkgconfig/libavcodec.pc || true
          ls -R ffmpeg/bin || true
          ls -R ffmpeg/lib || true

      - name: Build libavif
        run: |
          export LIBAVIF_ROOT="${PWD}/libavif"
          packaging/build_libavif_arm64.bat

      - name: Build wheel
        shell: cmd
        run: call packaging\vc_env_helper_arm64.bat packaging\build_wheel_arm64.bat

      - name: Repair and inspect wheel
        run: |
          bash packaging/post_build_script.sh
          ls -R
          ls -R dist || true
          ls -R wheelhouse || true

      - name: Upload wheel artifact
        uses: actions/upload-artifact@v4
        with:
          name: torchcodec-windows-arm64-py311
          path: |
            dist/*.whl
            wheelhouse/*.whl
          if-no-files-found: error
          retention-days: 14

      - name: Remove src/ folder
        run: bash packaging/remove_src.sh

      - name: Install torchcodec from the wheel
        run: bash packaging/install_torchcodec_wheel.sh

      - name: Install test dependencies
        run: bash packaging/install_test_dependencies.sh false

      - name: Run ARM64 smoke test
        run: |
          export PATH="${PWD}/ffmpeg/bin:${PATH}"
          export TORCHCODEC_FFMPEG_BIN_DIR="${PWD}/ffmpeg/bin"
          python packaging/assert_ffmpeg_version.py 9
          FAIL_WITHOUT_IMAGE_CODECS=0 FAIL_WITHOUT_HEIC=0 pytest --override-ini="addopts=-v" test/smoke_test.py -k "not decode_gif and not VideoEncoder and not AudioEncoder" --tb=short
